# C1–C5 full E2E continuation

Author: 0z5a. This record supersedes earlier TP2 status where explicitly noted.
SSH access has recovered. C1/C2/C4's small-model training-to-serving and
publication checks below are complete. C5 is running with explicit public-loader
and weight-retention audit; C3 still awaits upstream source. This is not an
all-pass report.

## Completed checks

| Scope | Result | Evidence / limit |
|---|---|---|
| C1 trained VeOmni checkpoint → converter → native serving | TP1/TP2 × eager/graph × target-only/draft generation passes; identical tokens | 25 logical checkpoint tensors match on every draft rank; graph replay observed on both TP2 ranks |
| C1 cached draft-logit replay | **Strict argmax gate fails** | All 64 calls satisfy atol 0.01 / rtol 0.02; maximum error 0.0078125. Eight calls differ in argmax, each with a tied maximum on at least one side. Compiled reference also fails the original gate |
| C2 head-dimension-64 fixture | Six two-rank optimizer updates and saves at 3/6 pass | Flat and partition checkpoints both pass TP2 serving, 25 tensors per rank, identical target-only tokens |
| C4 head-dimension-64 fixture | Six two-rank VeOmni optimizer updates and saves at 3/6 pass | Actual checkpoint passes TP1/TP2 eager/graph hot/cold publication; eager logits are exact on all ranks |
| C3 | Waiting for maintainer implementation | Coordination comment remains unanswered; no duplicate scheduler implemented |
| C5 original PR10 | EAGLE3 v13 running; DFlash v6 queued; v11/v4 completed first rollout then failed before optimizer update | Original production source is fixed at b258ec517977a01df722909789da42746c39f28f; no success inferred from older main-based runs |

The head-dimension-16 tiny target stalls at TP2 generation, while independent
NCCL/PyNCCL collectives, real Qwen3-4B TP2 generation, and a head-dimension-64 tiny
target pass. This narrows the failure to the old fixture/runtime combination;
it does not establish a general communication failure or a P-EAGLE regression.

## Speed comparison

Speed change is baseline time / candidate time − 1. These fresh-process totals
include startup, six updates, two saves and teardown. They are single observations
on a shared machine in flat → partition → VeOmni order, with cache and I/O drift;
they do not establish a speedup or an online RL performance improvement.

| Workload | Baseline | Candidate | Observed speed change | Conclusion |
|---|---:|---:|---:|---|
| Six-step standalone, flat → partition | 49.401 s | 34.019 s | +45.22% | Uncontrolled single run |
| Six-step standalone, FSDP2 → VeOmni | 49.401 s | 26.097 s | +89.30% | Uncontrolled single run |
| Frozen serving matrix | Identical prompt/output tokens | Identical prompt/output tokens | N/A | Correctness hooks; no timed performance claim |
| PR10 full online RL | Pending | Pending | N/A | No completed comparison |

## Environment and provenance

Serving / C5 use the L20 host's separate native environment: vLLM 0.29.0,
PyTorch 2.13.0+cu130, Python 3.13.14. Transformers was changed from 5.17.0 to
5.10.4 for verl compatibility. PR10 imports verl 0.8.0 from a no-deps overlay;
the standalone training runs use the previously pinned container environment.
The shared host vLLM 0.18 installation is unchanged.

C5's isolated vLLM overlay contains the previously tested idempotent EAGLE3 name
prefix guard and a dense-model import compatibility alias, `FusedMoE = RoutedExperts`.
The alias is not a MoE/FP8 validation. The old training loader receives a separate
config with `LlamaForCausalLMEagle3`; serving explicitly retains the original
`Eagle3LlamaForCausalLM` config and unchanged weights. These are local test
dependencies, not the PR author's requested companion vLLM commit.

Raw standalone logs and exit status are in `evidence/l20-20260920/full-e2e/`.
C1's separate checkout contains all eight serving JSONs, logs and the complete
64-call failure diagnostic. The earlier positive C2 optimizer-step result remains
a microbenchmark, not a replacement for this E2E table.

## Cleanup

Four completed head-dimension-16 fixture weight files were removed, freeing
2,271,968 bytes. Paths, sizes and hashes are in
`evidence/l20-20260920/full-e2e/completed-head16-cleanup.json`.
An additional 27 completed head-dimension-64 model/optimizer files were removed, freeing 181,914,574 bytes. Real C5 target/drafter weights remain active. The cleanup manifest is committed with C1.

## Current execution state

C1 commit `e813ce5` contains the finished TP1/TP2 × eager/graph hot/cold matrix,
all-rank logits comparison and flat/partition checkpoint serving, with raw logs.
The test-side parallel-mask refresh fixes a stale cache error of 0.0185546875
while preserving its address. This does not add a production online P-EAGLE
adapter. The independent causal-reference argmax failure above remains recorded.
C4's fixture CLI update is `8298042`; C2 implementation remains `ae8b5a2`.

Remote root: `/home/kxqandccx/0z5a/speco-l20-20260919/c5-native-latest`.
Current C5 launcher PID: 3864596 (revalidate before acting).
Inspect `evidence/full-e2e/online-eagle-pr10-v13.{log,exit}` and then
`online-dflash-pr10-v6.{log,exit}`. The launcher runs them sequentially with
30-minute bounds on GPUs 0/1. Do not start duplicate jobs or remove real weights.

The earlier v8 attempt reached initial target synchronization but failed the
vLLM 0.29 tied-weight alias check: a bucket containing only `lm_head.weight`
lacked `model.embed_tokens.weight`. Its RPC then remained blocked. The stopped
process tree and original log are retained. The full transmitted target state,
including the alias, is 8,822,848,512 bytes; the resumed run uses a 9,216 MiB
actor bucket and a separate 128 MiB draft bucket. v9 was interrupted after this
size check; v10 was relocated when another job reduced GPU 4's free memory.

The original PR10 runtime SHA-256 is
`26da8226845e010375d890334ca98251282abe6e1498c67da7e1525685439977`.
The test overlay logs after the original public loader and hashes the private
`fc.weight` after publication and later target sync. It does not repair
retention failures or alter loader logic. A passing FC hash is a sampled private
weight check, not an all-parameter or derived-buffer oracle.

Startup mitigation is scoped to this task: 2,434 Transformers files were copied
to tmpfs and verified byte-identical. A fixed 235-package distribution mapping
is cached for new processes, avoiding repeated filesystem scans during imports.
Three existing Ray processes were restored from nice 15 to normal priority 0;
subsequent launches use `RAY_worker_niceness=0`. The shared environment and other
jobs are unchanged. These startup changes preclude a controlled timing claim.

Upstream main was rechecked at
`18dd7094c35d61a1710a73e8b3bd9630d0d0ffb3`; implementation checkouts remain
rebased on it. C5 intentionally retains the original PR10 base. Source formatting
checks pass; raw logs preserve their original whitespace.

Native v11/v4 both exited 1 at verl's missing `flash_attn.bert_padding` import.
The SDPA experiment now imports unchanged upstream PyTorch padding helpers from
an isolated verl overlay; forward values and backward gradients pass a masked
reference check. v12 was stopped to correct Ray working-directory precedence;
v13 starts in the audit overlay and logs actual runtime source paths. No completed
20-step online RL result or successful publication is claimed yet.
