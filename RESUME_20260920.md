# Saved execution state — 2026-09-20

Author: 0z5a. Continue all C1–C5 E2E work from existing results; do not start
duplicate GPU jobs. Reports contain failures and scope limits, not an all-pass claim.

## Local saved work

Sibling checkouts live under `/Users/0z5a/Documents/infra/0z5a/`.

| Checkout | Latest result commit before this note | State |
|---|---|---|
| speco-l20-20260919 | 5a35e3a | Full report saved; older untracked reference/evidence files remain on disk |
| speco-peagle-frozen-serving | e813ce5 | Clean; trained checkpoint serving and hot/cold matrix saved |
| speco-partition-e2e | ae8b5a2 | Clean; partition fix, tests and speed tables saved |
| speco-veomni-drafter | 8298042 | Clean; standalone training/resume evidence saved |
| speco-pr10-native-e2e | cf297c4 | Clean; harness, failure logs, audits and report saved |

These are local commits; no new remote push or PR was performed during this continuation.
Do not blanket-add or delete the main checkout's older untracked files.

C1/C2/C4 small-model train → checkpoint → native serving tests completed.
C1's strict causal-reference argmax gate still fails on BF16 ties, although
numeric tolerance checks and final target tokens pass. C3 still awaits the
maintainer's idle-worker implementation; the coordination comment is
https://github.com/verl-project/verl-SpeCo/issues/7#issuecomment-5746767473 .
PR #93 adjusts training budgets and does not supply that idle-worker implementation.

## Remote connection and processes

SSH: `ssh -oBatchMode=yes -oConnectTimeout=20 -p 6000 kxqandccx@viperekura.xyz`.
The old master `/tmp/speco-resume-c` disconnected. Fresh connections have refused
port 6000 since about 14:16 Asia/Shanghai. DNS resolved to 47.109.194.246;
GitHub access still worked. A question about a changed SSH entrance is pending.

Root: `/home/kxqandccx/0z5a/speco-l20-20260919`.
Native environment: `c5-native-latest/.venv/bin/python`, vLLM 0.29.0,
torch 2.13.0+cu130, Transformers 5.10.4, Python 3.13.14, verl 0.8.0 overlay.

Last known jobs, both on physical GPUs 0/1:

- PID **4101336**: original sequential wrapper, now running **DFlash v9**.
- PID **4173853**: waits for PID 4101336 to exit, then execs
  `variants/pr10-native/experiment/l20/run_native_full_audit_v17.sh eagle`.
  This runs **EAGLE3 v17 only**, with a 256 MiB draft bucket.

Revalidate process identity before acting. The last confirmed DFlash state was
9 completed steps; step 10 was writing checkpoints, about 5.6 GiB present.
Do not assume either process survived the connection outage or finished.

Logs and per-run exit files are under `c5-native-latest/evidence/full-e2e/`:

- `online-dflash-pr10-v9.log`, `.exit`, `-summary.json`
- `online-eagle-pr10-v17.log`, `.exit`
- `queued-eagle-v17.log`
- `checkpoints-dflash-pr10/`, `checkpoints-eagle-pr10/`

The latest DFlash v9 log and checkpoints have **not been downloaded locally**.
Earlier EAGLE failure logs through v16 and their evidence are committed locally.

## C5 configuration and confirmed findings

Original production source is fixed at `b258ec517977a01df722909789da42746c39f28f`;
do not rebase it onto main. The native public-loader dependency overlay contains
the idempotent EAGLE name-prefix guard and dense-only FusedMoE import alias.
The retention audit only logs and fingerprints FC weights; it does not repair
the original loader or checkpoint-reload logic.

The driver must start in `pr10-retention-overlay-v2`, because Ray prepends its
working directory. Runtime source-path logs confirm the audit on actual workers.
The isolated padding overlay uses unchanged upstream PyTorch padding helpers.
Actor SDPA uses remove-padding, fixed microbatch 1, and no dynamic batching;
single-sequence padded/unpadded parity passed. Longer multiplication prompts
and a two-row minimum capture window exercise nonzero updates.

- EAGLE v15: actor grad norm 19.875, 2 draft samples, 1 draft update; CUDA IPC
  failed with `pidfd_getfd: Operation not permitted`.
- Shared-memory transport is supported by the original PR and selected now.
- EAGLE v16: 128 MiB bucket rejected its 156.25 MiB head; v17 uses 256 MiB.
- DFlash v9: 128 MiB is sufficient (largest published tensor 62.5 MiB).
  Both TP ranks repeatedly completed the native public loader with 58 tensors;
  every observed step trained, and published FC fingerprints changed.
- DFlash target sync replaces the latest FC with its initial checkpoint value.
  The raw checkpoint hash is `7ea10cdfe0c6ce0f3a39017d87ff0b82ac19ca37e5b64206d28e139b7e3ca673`.
  This reload exists in PR10's parent. Interval-1 republishing permits continued
  training, but the intermediate retention gate fails.

`experiment/l20/summarize_pr10.py` separates training/publication completion
from retention. Complete publication requires all-rank loads, all 20 steps and
clean exit through the original pre-rollout/final pending-RPC barriers.
Async `published=1` alone is insufficient. The FC check is sampled retention,
not an all-parameter or derived-buffer oracle. No controlled C5 speedup is claimed.

## Next steps after access returns

1. Read both exit files/logs and inspect the two process identities before launching anything.
2. Download the full logs, run the summary checker, and verify step-10/20 checkpoints.
3. Finish only missing work; preserve all failing retention evidence.
4. Commit final Markdown result/speed tables and raw evidence as 0z5a.
5. Clean completed task weights after their final consumers exit, recording paths,
   sizes and hashes. Real target/EAGLE/DFlash weights remain because these tests
   were active. Completed head64 weights (27 files, 181,914,574 bytes) were already
   removed and their manifest committed in C1. Preserve logs and compact captures.

Do not change host security policy, kill other users' GPU jobs, or post C5 comments
without user authorization. The latest verified upstream main remains
`18dd7094c35d61a1710a73e8b3bd9630d0d0ffb3`; the implementation checkouts are rebased.
