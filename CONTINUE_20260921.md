# C1–C5 continuation guide — 2026-09-21

Author: 0z5a

## Current state

C1, C2, C4 and C5 have completed their available E2E scopes. C3 cannot run because the reviewed source does not contain the required idle-worker/bubble implementation. The coordination thread is https://github.com/verl-project/verl-SpeCo/issues/7#issuecomment-5746767473 .

This is not an all-pass result:

- C1 passes generated-token and numeric-tolerance checks, but 8/64 strict cached-logit argmax comparisons differ on BF16 maximum ties.
- C5 completes training and publication, but all post-step retention checks show that target synchronization replaces the latest trained drafter with checkpoint weights.

## Saved repositories

All paths are under `/Users/0z5a/Documents/infra/0z5a/`.

| Scope | Directory | Commit | Result document |
|---|---|---|---|
| Overall | `speco-l20-20260919` | `e39d61275c9c` | `FULL_E2E_20260920.md` |
| C1 | `speco-peagle-frozen-serving` | `e813ce56a975` | `TEST_RESULTS.md` |
| C2 | `speco-partition-e2e` | `ae8b5a2688fc` | `TEST_RESULTS.md` |
| C4 | `speco-veomni-drafter` | `829804241112` | `TEST_RESULTS.md` |
| C5 | `speco-pr10-native-e2e` | `8e6fc8973c4c` | `TEST_RESULTS.md` |

All five commits are pushed to https://github.com/0z5a/verl-SpeCo and were verified against their remote hashes. The original upstream remote remains `https://github.com/verl-project/verl-SpeCo.git`; the writable remote is named `publish`.

## Final E2E results

| Claim | Result |
|---|---|
| C1 frozen serving | TP1/TP2 × eager/graph × target-only/draft completed; generated tokens match |
| C2 partition training | Six two-rank updates, step-3/6 checkpoints and TP2 serving completed |
| C3 bubble scheduling | Missing implementation; no duplicate scheduler was added |
| C4 VeOmni drafter | Six two-rank updates, checkpoint/resume and TP1/TP2 serving completed |
| C5 native online RL | Fresh DFlash and EAGLE3 runs each completed steps 1–20 and exited 0 |

## Speed table

| Workload | Baseline | Candidate | Observed result |
|---|---:|---:|---:|
| C2 six-step flat → partition | 49.401 s | 34.019 s | 45.22% lower time |
| C2 six-step FSDP2 → VeOmni | 49.401 s | 26.097 s | 47.17% lower time |
| C2 Qwen3-4B old partition → fixed | 2,021.38 ms | 1,692.40 ms | 16.28% lower time |
| C2 peak allocation old → fixed | 16.28 GiB | 14.23 GiB | 12.56% less memory |
| C5 DFlash → EAGLE3 steady step | 22.399 s | 14.397 s | 35.72% lower time; 1.556× |

The C2 standalone values are uncontrolled single runs on a shared machine. The C5 comparison uses different drafter architectures in one synthetic fixture and is not an isolated optimization benchmark.

## C5 evidence

C5 ran on two NVIDIA L40S GPUs with:

- Python 3.12.14
- PyTorch 2.13.0+cu129
- vLLM 0.29.0
- Transformers 5.17.0
- verl `7aed6b230776f963fa09509c10d9c3a767d1102c`
- tested source `cf297c4a1ce806c2bc6114483fc63b86ef24e589`

Evidence is in `speco-pr10-native-e2e/evidence/l40s-20260920/`. `SHA256SUMS` covers both successful logs, exit files, timing JSON, environment information, pre-cleanup model hashes and the cleanup manifest.

Both algorithms report 20 successful drafter training metrics and 20 successful publication metrics. Each has 38 `latest_draft_retained=False` checks and zero successful retention checks. Publication interval 1 permits continued training by loading a newly trained drafter after every target sync; it does not repair retention.

## Cleanup and safety state

No task-owned process remains on the L40S machine. After evidence was copied and verified, these task-owned objects were removed:

- `/workspace/0z5a/c5-native-latest/models` — 9.0 GB
- `/workspace/0z5a/c5-native-latest/eagle-training-alias`
- `/workspace/0z5a/speco-l40s/models` — task-only symlink

NFS was not touched. The machine was not restarted. Logs, manifests, source and environments remain available.

## Continue from here

1. Pull or fetch `https://github.com/0z5a/verl-SpeCo` and select the scope from the table above.
2. Read that checkout's `TEST_RESULTS.md` before changing code. For cross-scope status, read `FULL_E2E_20260920.md` in the overall checkout.
3. For C3, first inspect the coordination thread and upstream changes. Only run C3 after an actual idle-worker/bubble implementation appears.
4. For C5, fix drafter retention across target synchronization, then rerun both fresh 20-step cases with the same audit. A valid fix requires 38/38 retained checks per algorithm, steps 1–20, successful all-rank loads and exit 0.
5. Keep speed results in Markdown tables. Preserve raw logs and exit files. Clean only completed task-owned model files after evidence is local and checksummed.
6. Rebase applicable implementation work on the current upstream state before publication. Preserve C5's reviewed source base when reproducing the recorded result.

Do not add the older untracked reference files in `speco-l20-20260919` unless they become required evidence. No pull request was created for the final five pushed result branches.
